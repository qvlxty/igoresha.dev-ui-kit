import { Meta, Controls, Canvas, Story } from '@storybook/addon-docs/blocks';
import * as NavPanel from './NavPanel.stories'

<Meta of={NavPanel} />

<div style={{marginLeft: '70px'}}>
    # Пример NavPanel

    <Story>
    <NavPanel />
    </Story>

    ```tsx
    import { Link } from 'react-router'

    <NavPanel
        LinkElement={Link}
        links={[
            { to: '/root', icon: <AiOutlineOrderedList size={24} /> },
            'Separator',
            { onClick: () => alert('Hello'), icon: <AiOutlineAccountBook size={24} /> },
            'Separator',
            { to: '/root', icon: <AiOutlineAccountBook size={24} /> },
        ]}
    
    />
    ```
</div>